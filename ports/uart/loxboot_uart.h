/*
 * loxboot_uart.h is deprecated.
 * Use include/loxboot/loxboot_transport.h instead, which declares:
 *   - loxboot_uart_session_t
 *   - loxboot_uart_run_session()
 *   - loxboot_uart_send_status()
 *   - loxboot_crc16()
 *
 * Implementation: ports/uart/loxboot_uart.c
 */
#ifndef LOXBOOT_UART_INTERNAL_H
#define LOXBOOT_UART_INTERNAL_H

#include "loxboot/loxboot_transport.h"

#endif
